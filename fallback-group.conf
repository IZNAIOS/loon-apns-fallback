# 这是针对上传配置定制的补丁；将下一行加入主配置的 [Proxy Group]。
# SUB_A / SUB_B 是原配置中已经存在的两个私密远程订阅标签，仓库不包含其 URL。
# DIRECT 是所有订阅节点不可用时的安全出口。
APNS-FALLBACK = fallback, SUB_A, SUB_B, DIRECT, url = http://captive.apple.com/hotspot-detect.html, interval = 60
